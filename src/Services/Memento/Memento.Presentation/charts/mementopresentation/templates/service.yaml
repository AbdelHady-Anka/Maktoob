apiVersion: v1
kind: Service
metadata:
  name: {{ template "mementopresentation.fullname" . }}
  labels:
    app: {{ template "mementopresentation.name" . }}
    chart: {{ template "mementopresentation.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "mementopresentation.name" . }}
    release: {{ .Release.Name }}
