apiVersion: v1
kind: Service
metadata:
  name: {{ template "messengerpresentation.fullname" . }}
  labels:
    app: {{ template "messengerpresentation.name" . }}
    chart: {{ template "messengerpresentation.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "messengerpresentation.name" . }}
    release: {{ .Release.Name }}
