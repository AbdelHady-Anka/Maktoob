apiVersion: v1
kind: Service
metadata:
  name: {{ template "identitypresentation.fullname" . }}
  labels:
    app: {{ template "identitypresentation.name" . }}
    chart: {{ template "identitypresentation.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "identitypresentation.name" . }}
    release: {{ .Release.Name }}
